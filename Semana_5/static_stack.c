// Static stack
#include <stdio.h>
#include <stdlib.h>

#define MAXN 500

typedef struct snode{
    int value;
} SNode;

// create
// push
// pop
// len
// sprint
// top
// empty

// sort
// union
// invert

int main()
{
    // initialize stack
    SNode stack = malloc(sizeof(SNode) * MAXN);
    int top;
    
    return 0;
}